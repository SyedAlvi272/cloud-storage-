#!/usr/bin/env sh
DIRNAME=$(dirname "$0")
[ -z "$DIRNAME" ] && DIRNAME="."
if [ -n "$JAVA_HOME" ] ; then
    JAVACMD="$JAVA_HOME/bin/java"
else
    JAVACMD="java"
fi
exec "$JAVACMD" -jar "$DIRNAME/gradle/wrapper/gradle-wrapper.jar" "$@"